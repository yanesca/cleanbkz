SET(TARGET_NAME boundtool)
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/include)
LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})

ADD_EXECUTABLE(${TARGET_NAME} main.cpp)
TARGET_LINK_LIBRARIES(${TARGET_NAME} cleanbkz ntl)
